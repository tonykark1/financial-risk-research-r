# Static dependency declarations for renv. These packages are part of the
# supported project interface even when the executed pipeline uses faster
# data.table/Quarto paths internally.
if (FALSE) {
  library(dplyr)
  library(rmarkdown)
  library(tarchetypes)
}
